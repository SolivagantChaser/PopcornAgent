# 最开始设计的原版配置，仅保留服务端口，无任何冗余配置
server:
  port: 8080 # 服务端口（默认8080）
spring:
  application:
    name: agent-java-core # 应用名称
logging:
  level:
    com.agent.core: info # 项目日志级别
    io.grpc: warn # gRPC日志级别（屏蔽冗余日志）